PORT=7100
MONGODB_URI=mongodb://127.0.0.1:27017/alumni_management

# Change this in production
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=7d

# Frontend dev server origin (CORS)
CORS_ORIGIN=https://alumni.nidwa.com,http://localhost:5173,http://localhost:5174

# Upload settings: local | cloudinary
UPLOAD_PROVIDER=local

# Optional (only when UPLOAD_PROVIDER=cloudinary)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=alumni-management

# Optional seed overrides (npm run seed)
SEED_ADMIN_NAME=System Admin
SEED_ADMIN_EMAIL=admin@alumni.local
SEED_ADMIN_PASSWORD=Admin@123
SEED_STUDENT_COUNT=50
